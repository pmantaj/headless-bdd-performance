language: ruby

rvm:
  - 2.2.1

sudo: false

before_script:
  - bundle exec rake db:create
  - bundle exec rake db:migrate

deploy:
  provider: heroku
  api_key:
    secure: $HEROKU_ENCRYPTED_API_KEY
